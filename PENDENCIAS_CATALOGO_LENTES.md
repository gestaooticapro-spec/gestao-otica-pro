# Pendencias de Catalogo de Lentes

Este arquivo registra itens que ainda nao conseguimos casar com seguranca no catalogo atual.

## Pendencias Abertas

Nenhuma no momento.

## Observacoes (Resolvidos / Modelagem)

- `Varilux Comfort Essilor Fit Eyecode` e `Varilux Comfort short Eyecode` nao aparecem como ofertas isoladas nesta tabela. Foram modeladas como **customizacoes** em `global_lens_offers.features.customizations` (com `engraving_code` `F`/`S` e regras como `min_fitting_height_mm` e `add_max`), entao nao sao mais pendencias.
- Sobre `Varilux Physio`: nas paginas 15/16 desta tabela o que aparece e `VARILUX Physio Extensee` (familia e ofertas no banco). Nao existe `Varilux Physio` "puro" nessa tabela.

## Como usar

- Quando uma nova pagina vier para auditoria, adicionar os nomes que nao baterem com o catalogo.
- Se depois a linha for identificada/modelada, mover a anotacao para "Observacoes" ou remover.

